use('usuarios_login');
db.createCollection('user',{
    validator:{
        $jsonSchema:{
            bsonTipe:"object",
            required:["username","password"],
            properties:{
                username:{
                    bsonType:"string"
                },
                password:{
                    bsonTipe:"password"
                }
            }
        }
    }
});

use('usuarios_login');
db.user.insertOne({
    username:"Migel",
    password:"MigelTrainer"
});